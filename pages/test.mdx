import { Normalize, Grid, Row, Col, Button } from '@smooth-ui/core-sc';
import Menu from '../components/Menu';
import Footer from '../components/Footer';

import BrowserDemo from '../components/BrowserDemo';
import Head from 'next/head';
import Highlight, { defaultProps } from 'prism-react-renderer';
export default ({ children }) => (
  <>
    <Head>
      <title>Home</title>
      <meta name='viewport' content='initial-scale=1.0, width=device-width' />
    </Head>
    {children}
  </>
);

# Test

This page serves as my playground for rendering and deploying react
components.

<BrowserDemo>
  <div>Here's a browser emulation in your browser.</div>
</BrowserDemo>

# Codeblock with syntax highlighting

Using [`prism-react-renderer`](https://github.com/FormidableLabs/prism-react-renderer) with the [github theme](https://github.com/FormidableLabs/prism-react-renderer/blob/master/themes/github.js)

```
export default ({ children }) => (
  <>
    <Head>
      <title>Home</title>
      <meta name='viewport' content='initial-scale=1.0, width=device-width' />
    </Head>
    {children}
  </>
);

```

## Live code editting

```js live=true
<div>
  <button>Change me </button>
</div>
```

.
